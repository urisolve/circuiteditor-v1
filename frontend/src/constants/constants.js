export const constants = Object.freeze({
  // UI
  CANVAS_SIZE: { width: 2560, height: 1440 }, // 16:9 resolution (1440p)
  CARD_HEIGHT: 300,
  CARD_WIDTH: 300,
  DRAWER_SIZE: 300,
  TOOLBAR_SIZE: 8,

  // Schematic
  ROTATION_INCREMENT: 45,
});
